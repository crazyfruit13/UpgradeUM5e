
Hooks.once("init", () => {
  CONFIG.DND5E.itemProperties.laser = {
    label: "Laser",
    isPhysical: "true"
  };
  CONFIG.DND5E.validProperties.weapon.add("laser");
});

Hooks.once("init", () => {
  CONFIG.DND5E.itemProperties.onehanded = {
    label: "One Handed",
    isPhysical: "false"
  };
  CONFIG.DND5E.validProperties.weapon.add("onehanded");
});

Hooks.once("init", () => {
  CONFIG.DND5E.itemProperties.double = {
    label: "Double",
    isPhysical: "false"
  };
  CONFIG.DND5E.validProperties.weapon.add("double");
});

Hooks.once("init", () => {
  CONFIG.DND5E.itemProperties.legacy = {
    label: "Legacy",
    isPhysical: "false"
  };
  CONFIG.DND5E.validProperties.weapon.add("legacy");
});

Hooks.once("init", () => {
  CONFIG.DND5E.itemProperties.masterwork = {
    label: "Masterwork",
    isPhysical: "false"
  };
  CONFIG.DND5E.validProperties.weapon.add("masterwork");
});

Hooks.once("init", () => {
  CONFIG.DND5E.itemProperties.superheavy = {
    label: "Super Heavy",
    isPhysical: "false"
  };
  CONFIG.DND5E.validProperties.weapon.add("superheavy");
});

Hooks.once("init", () => {
  CONFIG.DND5E.itemProperties.ap = {
    label: "AP",
    isPhysical: "true"
  };
  CONFIG.DND5E.validProperties.weapon.add("ap");
});

Hooks.once("init", () => {
  CONFIG.DND5E.itemProperties.augment = {
    label: "Augment",
    isPhysical: "false"
  };
  CONFIG.DND5E.validProperties.weapon.add("augment");
});

Hooks.once("init", () => {
  CONFIG.DND5E.itemProperties.auto = {
    label: "Auto",
    isPhysical: "false"
  };
  CONFIG.DND5E.validProperties.weapon.add("auto");
});

Hooks.once("init", () => {
  CONFIG.DND5E.itemProperties.autoheavy = {
    label: "Auto-Heavy",
    isPhysical: "false"
  };
  CONFIG.DND5E.validProperties.weapon.add("autoheavy");
});

Hooks.once("init", () => {
  CONFIG.DND5E.itemProperties.breech = {
    label: "Breech",
    isPhysical: "false"
  };
  CONFIG.DND5E.validProperties.weapon.add("breech");
});

Hooks.once("init", () => {
  CONFIG.DND5E.itemProperties.direct = {
    label: "Direct",
    isPhysical: "false"
  };
  CONFIG.DND5E.validProperties.weapon.add("direct");
});

Hooks.once("init", () => {
  CONFIG.DND5E.itemProperties.esp = {
    label: "ESP",
    isPhysical: "false"
  };
  CONFIG.DND5E.validProperties.weapon.add("esp");
});

Hooks.once("init", () => {
  CONFIG.DND5E.itemProperties.explosive = {
    label: "Explosive",
    isPhysical: "false"
  };
  CONFIG.DND5E.validProperties.weapon.add("explosive");
});

Hooks.once("init", () => {
  CONFIG.DND5E.itemProperties.feed = {
    label: "Feed",
    isPhysical: "false"
  };
  CONFIG.DND5E.validProperties.weapon.add("feed");
});

Hooks.once("init", () => {
  CONFIG.DND5E.itemProperties.grenade = {
    label: "Grenade",
    isPhysical: "false"
  };
  CONFIG.DND5E.validProperties.weapon.add("grenade");
});

Hooks.once("init", () => {
  CONFIG.DND5E.itemProperties.guided = {
    label: "Guided",
    isPhysical: "false"
  };
  CONFIG.DND5E.validProperties.weapon.add("guided");
});

Hooks.once("init", () => {
  CONFIG.DND5E.itemProperties.magnetic = {
    label: "Magnetic",
    isPhysical: "false"
  };
  CONFIG.DND5E.validProperties.weapon.add("magnetic");
});

Hooks.once("init", () => {
  CONFIG.DND5E.itemProperties.nuclear = {
    label: "Nuclear",
    isPhysical: "True"
  };
  CONFIG.DND5E.validProperties.weapon.add("nuclear");
});

Hooks.once("init", () => {
  CONFIG.DND5E.itemProperties.organic = {
    label: "Organic",
    isPhysical: "false"
  };
  CONFIG.DND5E.validProperties.weapon.add("organic");
});

Hooks.once("init", () => {
  CONFIG.DND5E.itemProperties.pincher = {
    label: "Pincher",
    isPhysical: "false"
  };
  CONFIG.DND5E.validProperties.weapon.add("pincher");
});

Hooks.once("init", () => {
  CONFIG.DND5E.itemProperties.plasma = {
    label: "Plasma",
    isPhysical: "false"
  };
  CONFIG.DND5E.validProperties.weapon.add("plasma");
});

Hooks.once("init", () => {
  CONFIG.DND5E.itemProperties.shotgun = {
    label: "Shotgun",
    isPhysical: "false"
  };
  CONFIG.DND5E.validProperties.weapon.add("shotgun");
});

Hooks.once("init", () => {
  CONFIG.DND5E.itemProperties.sniperplusone = {
    label: "Sniper +1",
    isPhysical: "false"
  };
  CONFIG.DND5E.validProperties.weapon.add("sniperplusone");
});

Hooks.once("init", () => {
  CONFIG.DND5E.itemProperties.sniperplusone = {
    label: "Sniper +1",
    isPhysical: "false"
  };
  CONFIG.DND5E.validProperties.weapon.add("sniperplusone");
});

Hooks.once("init", () => {
  CONFIG.DND5E.itemProperties.sniperplustwo = {
    label: "Sniper +2",
    isPhysical: "false"
  };
  CONFIG.DND5E.validProperties.weapon.add("sniperplustwo");
});

Hooks.once("init", () => {
  CONFIG.DND5E.itemProperties.sonic = {
    label: "Sonic",
    isPhysical: "false"
  };
  CONFIG.DND5E.validProperties.weapon.add("sonic");
});

Hooks.once("init", () => {
  CONFIG.DND5E.itemProperties.undermount = {
    label: "Undermount",
    isPhysical: "false"
  };
  CONFIG.DND5E.validProperties.weapon.add("undermount");
});

Hooks.once("init", () => {
  CONFIG.DND5E.itemProperties.spp = {
    label: "SPP",
    isPhysical: "false"
  };
  CONFIG.DND5E.validProperties.weapon.add("spp");
});

Hooks.once("init", () => {
  CONFIG.DND5E.itemProperties.physical = {
    label: "Physical",
    isPhysical: "false"
  };
  CONFIG.DND5E.validProperties.weapon.add("physical");
});

Hooks.once("init", () => {
  CONFIG.DND5E.itemProperties.detonator = {
    label: "Detonator",
    isPhysical: "false"
  };
  CONFIG.DND5E.validProperties.weapon.add("detonator");
});

Hooks.once("init", () => {
  CONFIG.DND5E.itemProperties.ignition = {
    label: "Ignition",
    isPhysical: "false"
  };
  CONFIG.DND5E.validProperties.weapon.add("ignition");
});

Hooks.once("init", () => {
  CONFIG.DND5E.itemProperties.sympy = {
    label: "Symp-Y",
    isPhysical: "false"
  };
  CONFIG.DND5E.validProperties.weapon.add("sympy");
});

Hooks.once("init", () => {
  CONFIG.DND5E.itemProperties.sympn = {
    label: "Symp-N",
    isPhysical: "false"
  };
  CONFIG.DND5E.validProperties.weapon.add("sympn");
});

Hooks.once("init", () => {
  CONFIG.DND5E.itemProperties.draw = {
    label: "Draw",
    isPhysical: "false"
  };
  CONFIG.DND5E.validProperties.weapon.add("draw");
});

Hooks.once("init", () => {
  CONFIG.DND5E.itemProperties.drum = {
    label: "Drum",
    isPhysical: "false"
  };
  CONFIG.DND5E.validProperties.weapon.add("drum");
});

Hooks.once("init", () => {
  CONFIG.DND5E.itemProperties.ftf = {
    label: "FTF",
    isPhysical: "false"
  };
  CONFIG.DND5E.validProperties.weapon.add("ftf");
});

Hooks.once("init", () => {
  CONFIG.DND5E.itemProperties.gyro = {
    label: "Gyro",
    isPhysical: "false"
  };
  CONFIG.DND5E.validProperties.weapon.add("gyro");
});

Hooks.once("init", () => {
  CONFIG.DND5E.itemProperties.percussion = {
    label: "Percussion",
    isPhysical: "false"
  };
  CONFIG.DND5E.validProperties.weapon.add("percussion");
});

Hooks.once("init", () => {
  CONFIG.DND5E.itemProperties.flintlock = {
    label: "Flintlock",
    isPhysical: "false"
  };
  CONFIG.DND5E.validProperties.weapon.add("flintlock");
});

Hooks.once("init", () => {
  CONFIG.DND5E.itemProperties.attackplusone = {
    label: "Attack +1",
    isPhysical: "false"
  };
  CONFIG.DND5E.validProperties.weapon.add("attackplusone");
});

Hooks.once("init", () => {
  CONFIG.DND5E.itemProperties.attackplustwo = {
    label: "Attack +2",
    isPhysical: "false"
  };
  CONFIG.DND5E.validProperties.weapon.add("attackplustwo");
});

Hooks.once("init", () => {
  CONFIG.DND5E.itemProperties.attackplusthree = {
    label: "Attack +3",
    isPhysical: "false"
  };
  CONFIG.DND5E.validProperties.weapon.add("attackplusthree");
});

Hooks.once("init", () => {
  CONFIG.DND5E.itemProperties.antimissile = {
    label: "Anti-Missile",
    isPhysical: "false"
  };
  CONFIG.DND5E.validProperties.weapon.add("antimissile");
});

Hooks.once("init", () => {
  CONFIG.DND5E.itemProperties.warmupone = {
    label: "Warm-Up 1",
    isPhysical: "false"
  };
  CONFIG.DND5E.validProperties.weapon.add("warmupone");
});

Hooks.once("init", () => {
  CONFIG.DND5E.itemProperties.warmuptwo = {
    label: "Warm-Up 2",
    isPhysical: "false"
  };
  CONFIG.DND5E.validProperties.weapon.add("warmuptwo");
});

Hooks.once("init", () => {
  CONFIG.DND5E.itemProperties.warmupthree = {
    label: "Warm-Up 3",
    isPhysical: "false"
  };
  CONFIG.DND5E.validProperties.weapon.add("warmupthree");
});